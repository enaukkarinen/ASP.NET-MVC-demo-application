@model WJOArvioitava.Models.Ostos

@{
    ViewBag.Title = "Poista";
}

<h2>Poista</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Ostos</h4>
    @Html.HiddenFor(model => model.Id)
    @Html.HiddenFor(model => model.TuoteId)
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Tuote
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Tuote.Nimi)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Maara)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Maara)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.YksikkoHinta)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.YksikkoHinta)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Poista" class="btn btn-default" /> |
            @Html.ActionLink("Takaisin", "Index")
        </div>
    }
</div>
